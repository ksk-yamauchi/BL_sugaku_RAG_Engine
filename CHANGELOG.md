## 履歴とバージョン管理 (Changelog)

本ドキュメントは、スタサプRAGエンジンおよび次世代AIチューターシステムの機能追加、変更、修正の履歴を管理します。[cite: 21]
バージョニングは「Keep a Changelog」の思想に基づき、各フェーズのスクリプト群および出力データの仕様変更を記録します。[cite: 21]

---

## [13.5.0] - 2026-08-09
### 🚀 Added (追加)
* **[Backend] 動画カタログ (global_video_catalog) の統合**:
  * `build_vector_db.py` にて、各PARTの `lecture_map.json` に存在する「動画のチャプター・セグメントの生データ」をかき集め、`global_video_catalog` という新しいキーでデータベースに直接統合。
  * これにより、AIの「概念へのアライメント（紐づけ）」から漏れてしまった動画情報も捨てずに100%保持可能となり、フロントエンドにおける「動画のタイムライン・チャプター表示機能」の基盤が完成。
* **[Frontend] タイムスタンプ・チャプター連動機能 (app.py Ver 4.0〜)**:
  * 動画カタログデータを利用し、UI上で「先生の解説要約（狙い）」を見ながら、特定のタイムスタンプ（例: `01:05〜`）をピンポイントで再生開始できるシークバー連動型のUIモックを実装。
* **[Frontend] 知識群（グループ）の視覚的オントロジー表現**:
  * Graph RAGのネットワークUIにおいて、レイアウトを階層型（上から下へ）に強制し、ノードのShape（四角形や六角形）と色を「基礎知識」「視点・条件」「再構成知識」「タスク」に振り分けることで、人間が直感的に「学習の枠組み（グループ）」を認知できるUIへ進化。
* **[Phase 1] 数学的厳格性と汎化防止ルールの追加 (Ver 13.1.8)**:
  * 「整式」を「多項式」と同義語として統合してしまうAIの過剰忖度（ハルシネーション）を防ぐため、プロンプトに「数学的定義が異なるものは厳格に分離する」という境界線ルールを追加。これにより、数学的正確性と表記揺れ防止の完全な両立を実現。
* **[Phase 2] LaTeX抽出のノイズ完全防止ルール (Ver 2.5.11)**:
  * `blackboard_ocr` 等において、文章中であっても数式は必ず `$` で囲み、無駄な改行やバックスラッシュを残さない絶対ルールをプロンプトに追加し、フロントエンドでの文字化けや描画エラーを根絶。

---

## [13.4.0] - 2026-08-09
### 🚀 Added (追加)
* **[Phase 1] 記憶の継承 (Ontology Memory Injection)**:
  * 単元（PART）を跨いだ解析時に発生する表記揺れ（エンティティの分裂）を完全に防止するため、Phase 1 実行時に `knowledge_master.json` と `task_master.json` から既存の抽出済み概念名リストを動的に取得し、プロンプトへ注入（インジェクト）する機構を実装。[cite: 21]
  * AIに対し「既存の用語と同義なら絶対に新語を作らず使い回す」という絶対ルールを課すことで、全カリキュラムを通じてオントロジーが1つのツリーに美しく収束するよう高度化。[cite: 21]
* **[Phase 2] 中問・小問対応の階層アセンブリ機構**:
  * トピック名の抽出において、大問番号だけでなく `[1]`（中問）、`(1)`（小問）、`(i)`（枝問）といった複雑な番号体系を個別に安全に抽出し、本文からノイズを削除した上で「大問 ➔ 中問 ➔ 小問 ➔ トピック本文」の順番で美しく再組み立て（アセンブリ）するロジックを実装。[cite: 21]
* **[Backend] オートワイヤリング（実体結合）機能**:
  * `build_vector_db.py` にて、AIが抽出漏れを起こした場合でも、子ノードの「親概念」が既存の知識ノード名と完全一致した場合、システム側で強制的に `subsumes`（親➔子）および `part_of`（子➔親）のエッジを自動生成するロジックを追加。ネットワークの断線を防ぎ、Graph RAGの精度を向上。[cite: 21]
* **[Obsidian] スマートリンクと重複ハブの排除**:
  * `export_global_obsidian_vault_mext.py` にて、指定された親概念が「実在するノード」であった場合、ダミーのハブページ（`【親概念】...`）を生成せず、実体ノードへ直接リンクを流し込む「スマートリンク機能」を実装。[cite: 21]
* **[Obsidian] 逆引きリストの自動追記**:
  * 各知識ノードのMarkdown内に、「自分を親概念として指定している下位概念（子ノードたち）」のリストを動的に集計し、自動で追記する機能を搭載。[cite: 21]

### 🔄 Changed (変更)
* **[Phase 1, 2] JSON Schema による出力の強制制御（完全浄化）**:
  * LLMへの指示（プロンプト）だけでは防ぎきれない「LaTeXの `$` 囲み忘れ」や「`\text{[ア]}` といったノイズの混入」、「改行コードの消失」を防ぐため、APIリクエスト時の `response_schema` 内の `description` で直接出力形式を縛り付けるハード制約へ移行。これにより、無理な正規表現を使わずにAIの出力自体を根本からピカピカに浄化することに成功。[cite: 21]
* **[Frontend & Obsidian] Explainable AI (XAI) に基づく解説要約の優先表示**:
  * `app.py` および Obsidian の動画サジェストUIにおいて、単なる数式の羅列である `blackboard_ocr` ではなく、先生のティーチング意図を言語化した `explanation_summary` を前面に表示するよう仕様変更。生徒が「なぜこの動画を見るべきか」を直感的に納得できるUXを実現。[cite: 21]

### 🛠️ Fixed (修正)
* **[Phase 2] トピック名のノイズ（問1等）除去強化**:
  * 動画解析時のトピック名に「問1」などの不要なプレフィックスが混入する現象を解決するため、正規化処理（クレンジング）のパターンを追加・強化。[cite: 21]

---

## [13.3.0] - 2026-08-07
### 🚀 Added (追加)
* **[Backend] 新世代ベクトルDB構築スクリプトの抜本的アーキテクチャ刷新**:
  * `build_vector_db.py` を改修し、単なるノードの羅列から「教育的・認知的オントロジーを体現するDB」へと構造を完全アップデート。[cite: 21]
  * **三つの柱と役割のメタデータ化**: ノードの型（基礎知識、視点・条件、再構成知識、タスク）に応じて、文科省の「知識及び技能」「思考力・判断力等」といった Pillar 属性と詳細な Role（役割）を付与し、Semantic Zoom（FTダッシュボード）での分析基盤を確立。[cite: 21]
  * **Graph RAG爆速化のためのエッジ構造化**: `incoming_edges` と `outgoing_edges` を単なる配列から、9つの関係性（`prerequisite`, `applies_condition` 等）ごとの辞書型グループへ変更し、フロントエンド側での「躓き原因の逆引き推論」を容易化。[cite: 21]
  * **Embeddingレシピの動的分岐**: 知識ノードには「公式解説」や「講義要約」を、タスクノードには「具体的な問題文と解説」や「板書OCR（数式）」を裏側で強力にブレンドしてベクトル化する仕様に変更。これにより、画像検索（数式等）において目的のタスクノードがダイレクトにヒットするよう検索精度を劇的に向上。[cite: 21]
  * **API堅牢化機能の統合**: 数千件におよぶDB構築時の429エラーを防ぐため、「複数APIキーの自動ローテーション」と「最新Embeddingモデル（text-embedding-004等）の自動探索機能」を統合実装。[cite: 21]
* **[System] 指導要領マスター V2 への対応**:
  * DB構築時に `mext_master_dict.v2.json` を読み込み、三つの柱（Pillar）ごとの詳細な解説文を各ノードに結合する処理を実装。[cite: 21]

---

## [13.2.0] - 2026-08-07
### 🚀 Added (追加)
* **[Architecture] デュアルエンジン構造（GNN-KT × オントロジー）の完全分離と両立**:
  * グラフのノードを、GNN-KTで確率計算の主役となる「タスク（動詞：_Tノード）」と、それを支える「知識・視点（名詞：_Kノード）」に完全分離。[cite: 21]
  * `foundation_knowledge`（基礎知識）、`perspective_condition`（視点・条件）、`derived_knowledge`（再構成知識）、`tasks`（技能・タスク）の4分類からなる高度なオントロジーモデルを新規定義。[cite: 21]
* **[Phase 1] タスク抽出の細分化ガードレール**:
  * 行う操作（例：「係数と次数を特定する」）が同じでも、対象となる数式（例：「単項式」と「多項式」）が異なる場合は、GNN-KTでの別々の技能として追跡するため「必ず別々のタスクノードとして分割する」という強力な抽出ルールを追加。[cite: 21]
* **[Phase 3] マルチモーダル暗黙知の動的発見と補完 (Dynamic Ontology)**:
  * 動画の口頭解説や板書OCRデータから、テキスト教材には書かれていない「先生の暗黙知」や「新しい視点（レンズ）」をAIが動的に発見し、グラフの知識ノード（`_Kxxx`）として自動追加・結合する自己増殖機能を実装。[cite: 21]
* **[Phase 3] GNN-KT保護のためのタスク追加禁止ルール**:
  * 動的補完を行う際、Phase 1で構築された問題とのアライメント（確率計算モデル）を壊さないため、動画からの「新しいタスク（技能）の追加」をシステムプロンプトで完全に禁止（知識ノードの追加のみに制限）する安全装置を搭載。[cite: 21]
* **[Phase 3] 動画ロール連動による「粒度吸収」アライメント**:
  * UIの頭出し用に極細分割された Phase 2 の動画セグメント（方針、立式、計算など）について、`role` が `exercise_walkthrough` または `concept_application` の場合は、新しいタスクにばらばらに紐付けるのではなく、Phase 1の既存タスク内に「一連のプロセス（配列）」として束ねて吸収させるロジックを実装。[cite: 21]

### 🔄 Changed (変更)
* **[Phase 1] 2段階推論（In-Context Learning）プロンプトへの回帰**:
  * AIのアテンション（注意力）を最大化するため、抽出処理（Step 1）と問題アライメント処理（Step 2）を分離した堅牢な2段階プロンプトアーキテクチャを採用。[cite: 21]
* **[Phase 1] 用語グラウンディングの仕様変更**:
  * テキストへの完全拘束（`extracted_from`）を廃止し、「日本の高校数学（検定教科書レベル）の標準的な名称にグラウンディングさせる（独自の造語禁止）」という抽象的ガードレールへ変更。AIの推論の萎縮を防ぎつつ、大学数学用語などのハルシネーションを排除。[cite: 21]
* **[System] マスターファイルの分割管理**:
  * 従来の `concept_branch_master.json` を廃止し、新たに `knowledge_master.json`（知識用）と `task_master.json`（タスク用）の2ファイルに分割。それぞれ独立して自動採番とメタデータ管理を行う仕様へ移行。[cite: 21]

---

## [12.2.0] - 2026-08-04
### 🚀 Added (追加)
* **[System] 複数APIキーの自動ローテーション機構**:
  * `.env` ファイルにて `GEMINI_API_KEYS`（カンマ区切り）で複数キーを登録可能に拡張。[cite: 21]
  * 各Phaseスクリプト（0〜3）にて、APIの利用制限（HTTP 429）や無効なキー（HTTP 400）を検知した場合、待機せずに自動で次のAPIキーへ切り替えて処理を続行する堅牢なフェイルオーバーを実装。[cite: 21]
* **[System] 動的リトライ回数設定**:
  * 固定の最大リトライ回数（5回）を廃止し、登録されたAPIキーの数に応じて動的に最大リトライ回数を設定（例: `max(5, キー数 × 2)`）する仕様に変更。[cite: 21]
* **[Phase 1, 2, 3] JSON自動修復・パース機能**:
  * LLM（Gemini）がJSON出力時にLaTeXのバックスラッシュ（`\`）のエスケープを忘れたことに起因する `JSONDecodeError` のクラッシュを防ぐため、システム側でテキストを自動検知・修復（`\` ➔ `\\`）してからパースを行う安全装置（`generate_content_and_parse_json`）を全解析フェーズに搭載。[cite: 21]
* **[Phase 0, 2] ファイル再アップロード（リカバリー）機構**:
  * 動画（Phase 2）およびPDF（Phase 0）の解析中、APIキーが切り替わったことによって発生するクラウド上の一時ファイルへのアクセス権限エラー（HTTP 403 PERMISSION_DENIED）を自動検知。[cite: 21]
  * 403エラー発生時、無限ループに陥るのを防ぎ、自動で古いファイルを破棄して新しいキーで再アップロードからやり直す自己修復ループを実装。[cite: 21]
* **[Document] GNN-KTシミュレーション仕様書の追加**:
  * ペルソナ定義および、必須/補足の重み付き前提知識を用いた連鎖ペナルティのアルゴリズムをまとめた専用仕様書（`docs/08_mock_log_simulation_spec.md`）を新規作成。[cite: 21]
### 🔄 Changed (変更)
* **[System] 環境変数の強制上書き仕様**:
  * Pythonの `load_dotenv` において、ターミナルセッション等に残存する古い環境変数を確実に上書きするため `override=True` 設定を追加。さらに、BOM（`\ufeff`）やクォーテーションを自動除去するクレンジング関数を初期化処理に導入。[cite: 21]
* **[Phase 0, 2] クラウドキャッシュ削除の安定化**:
  * 処理完了時のファイル削除（`files.delete`）処理において、アップロード時と同一のクライアントインスタンスを保持して使用するよう変更し、キー切り替え時の不要な削除エラーを抑止。[cite: 21]
* **[Backend] GNN-KT検証用ダミーログ生成ロジックの高度化**:
  * `generate_mock_logs.py` において、最新のオントロジー構造に対応。必須前提（`mandatory`）未理解時は正答率×0.1、補足前提（`supplementary`）未理解時は×0.7とする連鎖ペナルティ計算を実装し、よりリアルなクラスの躓きデータを生成するよう改修。[cite: 21]

---

## [12.1.0] - 2026-08-04
### 🚀 Added (追加)
* **[Phase 1] 枝番マスターメタデータの拡張・自動蓄積**:
  * `concept_branch_master.json` の各概念ノードに、要約の冒頭（`summary_snippet`）および初出単元名（`first_appeared_in`）を追加保存する仕様へ変更。[cite: 21]
  * 後続の講（PART）の解析において過去の定義コンテキストを保持可能にし、全講を通したオントロジーの一貫性を向上。[cite: 21]
* **[Backend] シラバス可視化テストツールの対話型CLI化**:
  * ターミナル上でユーザーが直接シラバステキストを手入力し、紐づく「Level 3親概念 ➔ Level 4アクション ➔ 動画ファイルおよび問題数」をリアルタイムで繰り返し確認できる対話型検証スクリプト（`visualize_syllabus_content.py`）を追加。[cite: 21]
* **[Backend] GNN-KT検証用ダミーログ生成ツールの追加**: 
  * 統合データベース（`global_vector_db_cache.json`）のオントロジー構造を直接読み込み、架空の生徒ごとの正誤ログデータ（`mock_student_logs.json`）を自動生成する `generate_mock_logs.py` を新規作成。[cite: 21]
  * 「知識・技能偏重型」などのペルソナ設定や、必須前提知識の不理解による「連鎖エラー」の確率変動ロジックを搭載し、GNN-KTのネットワーク推論や適応型学習ナビの検証に耐えうるリアリティのあるデータをシミュレーション可能にした。[cite: 21]
* **[Document] 画像RAG専用プロンプト仕様書の追加**: 
  * マルチモーダル画像RAGにおいて、画像をスタサプのオントロジー（Level 3推論、前提知識、エラー分析）にマッピングするためのJSONスキーマとシステムプロンプトの設計書（`docs/06_image_rag_prompt_design.md`）を新規作成。[cite: 21]
* **[Document] シラバスマッチング専用プロンプト仕様書の追加**:
  * 学校独自のシラバス表記をスタサプオントロジー（Level 3）にマッピングするためのシステムプロンプト設計書（`docs/07_syllabus_mapping_prompt.md`）を新規作成。[cite: 21]
### 🔄 Changed (変更)
* **[Phase 1] In-Context Learning（Few-Shot）によるプロンプト構造改革**:
  * 枝番マスターに蓄積された過去の概念名と要約スニペットを、Phase 1（Step 1）実行時に「お手本データ」としてプロンプトへ動的注入するロジック（`load_branch_master_examples`）を搭載。[cite: 21]
 * AIの萎縮や解釈のブレを引き起こしていた「NGルール（〜するな）」や曖昧な定性表現（「日常的な授業で使われる」等）を完全撤廃し、ポジティブな形式ルール（`[対象]＋[具体的処理]`）と実例模倣のみで教科書レベルの概念名を抽出する仕様へ改修。[cite: 21]
---

## [12.0.0] - 2026-08-03
### 🚀 Added (追加)
* **[Phase 1] GNN-KTおよびSemantic Zoom用データ構造の追加**: 
  * 抽出概念（Level 4）に対して、学習観点タグ（`competency`: 知識・技能 / 思考力・判断力等）の自動分類を追加。[cite: 21]
  * 前提知識（`prerequisite_concepts`）を単純な文字列から、`dependency_type`（mandatory[必須] / supplementary[補足]）と `reasoning`（理由）を持つオブジェクト構造へ拡張。[cite: 21]
  
* **[Frontend] Graph RAG UIの拡張**:
  * `app.py` のスキルツリー（`agraph`）にて、必須前提（実線／濃い青）と補足前提（点線／水色）を視覚的に分離。[cite: 21]
  * `app.py` のサイドバーに、現在読み込んでいるDBのバージョン（`engine_version`）を表示するUIを追加し、データ仕様の視認性を向上。[cite: 21]
* **[Frontend] FT画面モックアップの追加とUI高度化**:
  * 先生向けダッシュボード `app_ft.py` を新規作成。階層型ヒートマップ（Treemap）を用いたSemantic Zoomのプロトタイプを実装。[cite: 21]
  * 評価指標を教育現場に即した「習熟度」に統一し、対象スコープ（学年・クラス等）に連動するAI根本原因アラートを実装。[cite: 21]
  * ヒートマップのホバー時に少し遅れて情報を表示するディレイ機能の仕様を追加し、Level 4のブロックをクリックした際に直下の「学習アクション・エクスプローラー（展開パネル）」が自動で開く（または内容が更新される）連動UIを設計。[cite: 21]
  * 「数学担当による全体分析（メインダッシュボード）」と「担任による面談時の個別配信（面談ダッシュボード）」の2つのユースケースを疑似体験できるタブ切り替え機能（状態保持つき）を実装。[cite: 21]
  * 既存システムの「STEP2: 配信設定」画面へのシームレスな遷移モックを追加。習熟度に基づく対象生徒の自動絞り込み、宿題タイプの自動選択、および遡り学習パック内容の可視化・編集（削除）機能を実装。[cite: 21]
* **[System] 共通環境構築ファイルの追加**:
  * プロジェクトルート（`BL_sugaku/`）に共通の `requirements.txt` を配置し、フロント・バックエンド両方の環境構築手順を統合・標準化。[cite: 21]
* **[Document] フロントエンド仕様書の統合・新規作成**:
  * 独立して存在していた `app.py` の仕様書と新規作成した `app_ft.py` の仕様を統合し、新たに `docs/05_frontend_applications.md` としてドキュメントを追加。[cite: 21]

### 🔄 Changed (変更)
* **[Phase 1] 前提知識の閉世界化（厳密化）**: プロンプトを改修し、前提知識として指定できる用語をLevel 3（親概念ハブ）レベルの一般数学用語に限定。表記揺れとグラフの分断を防止。[cite: 21]
* **[DB構築]**: `build_vector_db.py` を改修し、メタデータに観点タグと重み付き前提知識を包含してベクトル空間へ埋め込む仕様に変更。また、出力JSONのメタデータに `engine_version` を刻む仕様を追加。[cite: 21]
* **[Obsidian]**: `export_global_obsidian_vault_mext.py` を改修し、Level 3の親概念ハブノードを自動集計・出力する仕様に変更。ターミナル出力のバージョン表記も「Ver 12.0」へ更新。[cite: 21]
* **[Phase 0]**: `phase0_pdf_to_md.py` の開始ターミナルログに `[Phase 0 Ver 1.0]` と明記するよう変更。[cite: 21]

### 🛠️ Fixed (修正)
* **[Phase 3] OCRデータの引き継ぎ保証 (ファジーマッチ結合)**: Phase 2（Ver 2.5）で抽出された `blackboard_ocr` などのデータを、新しいPhase 1データと結合する際に欠落せず安全に引き継ぐ処理を実装。[cite: 21]

---

## [10.4.0] - 以前の主要アップデート
### 🚀 Added (追加)
* **[Phase 3] ファジーマッチ（最寄時間検索）安全装置**: タイムスタンプのミリ秒単位のズレや、欠落が発生した場合でも、一番近い時間のセグメントを自動で探して動画と概念を結合するフォールバックロジックを搭載。[cite: 21]

---

## [2.5.1] - 2026-08-03
### 🛠️ Fixed (修正)
* **[Phase 2] トピック名の正規化ロジック修正**: `concept_application`（応用・利用）の動画セグメントまで「[例題]」フォーマットに強制上書きされてしまう要件定義の誤りを修正。トピック名の正規化処理を `exercise_walkthrough`（例題演習）のみに限定し、正しい判定ログ（「例題と判定されたため...」）が出力されるよう改修。[cite: 21]

---

## [2.5.0] - 以前の主要アップデート
### 🚀 Added (追加)
* **[Phase 2] マルチモーダル超極細解析＆板書OCR**: Gemini 1.5 Pro (当時) / 3.6 Flash のマルチモーダル機能を活かし、動画内の黒板・スライドの数式をLaTeXとして書き起こす `blackboard_ocr` 機能を実装。[cite: 21]
* **[Phase 2] 動的役割推論（ロール判定）**: 教材テキストと連動し、講義動画の役割を `concept_lecture`（概念講義）、`exercise_walkthrough`（例題演習）、`concept_application`（応用・利用）の3つに自動分類するロジックを搭載。[cite: 21]