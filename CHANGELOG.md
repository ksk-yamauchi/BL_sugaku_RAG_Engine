# 履歴とバージョン管理 (Changelog)

本ドキュメントは、スタサプRAGエンジンおよび次世代AIチューターシステムの機能追加、変更、修正の履歴を管理します。
バージョニングは「Keep a Changelog」の思想に基づき、各フェーズのスクリプト群および出力データの仕様変更を記録します。

---

## [14.10.0] - 2026-08-16
### 🚀 Added (追加) & Changed (変更)
* **[Frontend] ハイブリッド純化アルゴリズムとUXの極限化 (Ver 4.19.2 - 4.19.10)**:
  * **適材適所の抽出フィルター分離**: 本命問題と次点（前提）問題で、抽出時に求める純化の思想を完全に分離し、それぞれに最適なアルゴリズムを適用。
  * **本命問題の純化（真のメタタスク判定）**: 複合問題を除外する際、混入している他タスクが「subsumes（包摂）以外のエッジを持つ真の再構成知識」を前提にしているか（応用か基礎か）を判定するロジックを導入。これにより「次数と定数項」や「代入式の計算」といった、基礎タスク同士の自然な複合問題を美しく許容。
  * **次点問題の純化（GNN到達可能性トラバーサル）**: 次点候補の問題を抽出する際、GNNの有向非巡回グラフ（DAG）の性質を利用し、本命タスクから過去（前提）のルートを再帰的に遡る `get_all_ancestor_nodes` を実装。前提ツリー上に存在しない「未来のタスク」や「別ルートの兄弟タスク（加減法など）」が混ざった問題を完全排除し、学習ルートの逸脱を防止。また、親カテゴリ経由での逆流を防ぐため、遡及を許可するエッジの種類を厳格に制限。
  * **トップヒット（デフォルト選択）の最適化**: ベクトル検索特有の微小なスコアのブレを吸収するため、問題のベクトル類似度を小数第2位で丸めて同着判定を実施。同着の場合は「問題番号が小さい方」を優先してデフォルト選択する、学習体験に即した自然なアルゴリズムへ刷新。
  * **講義名によるUIグループ化とボタン状態管理**: 本命問題の複数候補選択において、同名ラベルによるUIバグを回避するため、動的ボタン（`st.button`）によるステート管理へ移行。講義名（`lecture_name`）の文字列をそのまま活かした、視認性の高い階層型グループ化UIを実現。
  * **アクション間のシームレスな自動追従**: ユーザーが本命問題を切り替えた際、選択された問題のベクトルおよび講義名と最も関連性の高い「第一アクション（モデリング大問）」を動的に再計算し、解説動画付きの例題を即座に連動・切り替えるロジックを実装。
  * **前提ツリーの可視化機能**: UI下部に「AIが推論した前提ツリー (到達可能ルート可視化)」エキスパンダーを新設。裏側でGNNトラバーサルが構築した学習の道のりをJSON形式で直接確認できるデバッグ兼分析用UIを追加。

## [14.9.0] - 2026-08-15
### 🚀 Added (追加) & Changed (変更)
* **[Frontend] 問題解法ルートのアーキテクチャ大改修とGNNトラバーサル (Ver 4.18.0 - 4.18.4)**:
  * **タスク起点の新アーキテクチャ（検索の一元化）**: 問題解法ルートの検索起点を「問題データへの直接ヒット」から「タスク（概念ノード）」へ完全移行。検索意図を一度タスクで受け止め、そこから紐づく問題をGNNの力で引き当てる、最も堅牢で拡張性の高い設計へ統合。
  * **本命問題の2段階ソートと純化（レゴブロック設計）**: ヒットしたタスク傘下の問題群とクエリベクトルの類似度を再計算（2段階ソート）し、最適な問題を本命として決定。さらに、概念ルートと共通の「他タスク混入排除フィルター（プロモートされたタスクは例外）」を適用し、提示される問題の純度を統一。
  * **GNNトラバーサルによる前提アセスメント（次点）の厳格化**: 問題同士の曖昧なタスク配列比較を廃止。ヒットしたタスクノードの「入ってくる矢印（`prerequisite`）」をオントロジーから直接逆引きし、真の前提タスク・知識を特定。その前提を測る確認問題のみを次点として抽出。
  * **次点問題の純化・重複排除フィルター**: 前提問題を引き当てる際、「再構成知識が含まれる問題」や「複数タスクが混在する問題」といった応用要素を徹底排除。さらに、本命問題やモデリング大問として画面に出現済みの問題ID（`displayed_q_ids`）を管理し、重複表示を完全にシャットアウト。
  * **前提概念（UI描画用）の絞り込み**: 遡り学習として提示される次点の概念リストから、抽象的な「再構成知識」や「視点・条件」を除外し、純粋な基礎知識やタスクのみを提示して生徒のアクションを明確化（Graph RAGの描画用データとはスコープを分離し、ツリーの全体像は維持）。
  * **学習アクションの大規模再配置**: 問題解法ルートのアクションを「第一アクション：モデリング大問」「第二アクション：概念インプット講義」へ再配置。類題の横展開を廃止し、問題の解法を確認した後に根本の概念を復習する導線へ最適化。
  * **LaTeX保護とMarkdown改行パッチ**: 数式ブロック（`$$...$$`）や多重エスケープ（`\\\\`）を正規表現で保護しつつ、Markdown仕様に合わせた改行（行末スペース2つ付与）を安全に実行する専用ヘルパー関数（`format_text_for_markdown`）を実装。

## [14.8.0] - 2026-08-15
### 🚀 Added (追加) & Changed (変更)
* **[Frontend] 概念理解ルートの純度向上と自己解決サイクルの確立 (Ver 4.17.2)**:
  * **他タスク混入問題の排除（モデリング・アセスメントの純化）**: 概念理解ルートにおいて、ヒットした概念が「タスク」であった場合、そのタスク『以外』のタスク配列（`linked_task_names`）を持つ応用問題を第二・第三アクションの候補から完全に除外するロジックを実装。初学者が純粋な基礎問題だけに集中できるUXを実現。
  * **再構成タスクの例外処理**: ただし、ヒットした概念が「再構成知識」からプロモート（変換）されたタスクである場合は、性質上複数のタスクが混在する応用問題となるのが必然であるため、上記の除外フィルターをオフにする安全装置（セーフティ）を実装。
  * **次点（前提）からの再構成知識の除外**: GNNから取得する前提ノード群（次点リスト）から、抽象的な気づきである `derived_knowledge`（再構成知識）をスキップし、純粋な「基礎知識」と「視点・条件」のみを表示することで、生徒が遡るべきアクションを明確化。
  * **解答・解説のスマート・アコーディオン表示**: すべての問題ルート（本命、モデリング、横展開、次点）において、問題文の下部に「💡 解答・解説を見る」のエクスパンダーを新設。DBの `answer_text` から「【解答】」と「【解説】」を動的に正規表現で分割し、美しく表示するUI関数を実装。
  * **Markdown改行の強制レンダリング対応**: データベース上の `\n` がMarkdown上で無視される仕様を回避するため、描画直前にスペース2つ＋改行（`  \n`）へ置換するパッチ処理を問題文・解答解説テキストに適用。
  * **UIの意図明確化**: 概念ルートの次点のボタン名を「この概念について深く学ぶ」に変更し、クリック後の遷移意図を強化。また、概念ルートの確認問題から「解き方を見る（問題解法ルートへのワープ）」ボタンを削除し、インプット体験のコンテキストを一本化。

## [14.7.0] - 2026-08-14
### 🚀 Added (追加) & Changed (変更)
* **[Frontend] ハイブリッド検索（条件付きHyDE）とダイレクト・ルーティングの導入 (Ver 4.6.31)**:
  * **数式・画像検知型の条件付きHyDE**: ベクトル検索が持つ「数式構造の理解不足」を克服するため、検索クエリに数式記号が含まれる場合や画像検索時にのみLLMを自動発動。AIの事前推論（解法アプローチ）をクエリに合体（HyDE）させることで、圧倒的な検索精度を実現。純粋なテキスト検索時はAPIをバイパスし高速化を維持。
  * **ID指定ダイレクト・ルーティング（排他ワープ）**: 「解き方を見る」やGraph RAGのノードクリック遷移時、テキスト再検索によるベクトルのブレ（誤爆）を廃止。ノードIDを指定した絶対的な単独トップヒット遷移を実装し、他ルートへのノイズ流出を完全遮断。
  * **Graph RAG（スキルツリー）の視覚的洗練**: 
    * 必須/トリガーエッジの統合によるラベル重複の解消とツールチップのフルネーム表示対応。
    * 問題ルートにおいて、現在地（確認問題）へ向かう前提知識の矢印の向きを「知識➔問題」に逆転し、認知的な正しさを担保。
    * 問題ルートの周辺情報（看板概念への前提や親エッジ）を「薄い青色」にトーンダウンし、クリティカルパスを強調するUX改善。
  * **横展開（第二アクション）の抽出ロジック緩和と重複排除**: タスクの完全一致条件を緩和し、「測られるタスクが1つ以上共通、かつ必要な知識が完全一致」に変更。より教育的な類題サジェストを実現すると同時に、次点リストとの重複表示を排除。
  * **AI事前解説のUI復旧とLaTeXレンダリング修正**: バックエンドで生成されていた「AIチューターからのアプローチ解説」を検索結果上部へチャット風UIとして復旧。「なぜこの動画？」の理由テキストがコードブロック化してLaTeXレンダリングされないバグを修正。
  * **概念ルートの導線整理**: 概念インプット優先ルートの第二アクション（大問）から、問題ルートへ逸脱する「詳しく見る」ボタンを削除し、学習体験のコンテキストを一本化。

## [14.6.1] - 2026-08-13
### 🚀 Added (追加) & Changed (変更)
* **[Frontend] UIの連動性とスキャッフォールディングの高度化 (Ver 4.6.15)**:
  * **ラジオボタンによる動的再描画 (Graph RAGの表示バグ解消)**: 本命候補の切り替えをタブからラジオボタンに変更。Streamlitの仕様上、非表示タブ内でグラフの縮尺計算が狂う問題を、切り替え時の「強制再描画」によって完全解消し、常にGraph RAGが中央に美しく配置されるUXを実現。
  * **第一アクション（大問）の最適化**: 複数候補のタブ表示を廃止。代表概念の一致に加え、「本命の確認問題」と「大問」のベクトル間距離（コサイン類似度）を直接計算し、トップヒットする1件のみを抽出・表示する仕様へ洗練化。
  * **次点（前提確認）のアプローチ刷新とGNNトラバーサル**: ベクトルスコアのニアミス抽出を廃止し、本命問題の「測られるタスク」を起点とするアプローチへ変更。さらに前提が「再構成知識」であった場合、その構成要素（`part_of`）である基礎知識へ多段ホップ探索（トラバーサル）を行い、基礎タスクを測る確認問題を抽出する高度なGNN-KTロジックを実装。
  * **類題プレースホルダーの新設**: 次点の下部に、同レベルの演習枠として「類題」セクションを独立配置（将来アップデート用のプレースホルダー）。
  * **Graph RAGの全ルート解放とスコープ化**: 概念ルート限定だった下部の「Graph RAG」および「前提・兄弟・Next」の3カラムを問題ルートにも解放。さらにこれらをラジオボタン（本命問題）のスコープ内にカプセル化し、問題切り替えに完全に追従するよう改修。

## [14.6.0] - 2026-08-12
### 🚀 Added (追加)
* **[Frontend] AIチューターUIのメジャーアップデート (Ver 4.6 Array Ready)**:
  * **多段・配列引き当てロジックの導入**: 単一の代表概念（`matched_concept`）への依存を脱却し、問題データが保持するタスク配列（`linked_task_names`）と知識配列（`linked_knowledge_names`）の包含関係を活用した確実な引き当て機構を実装。
  * **タスク駆動型の類題サジェスト（横展開）**: 問題ルートの「第二アクション（同概念の横展開演習）」において、単に知識が同じ問題ではなく、「同じタスク（技能）を測る問題」を優先して類題として結びつける教育的アルゴリズムを採用。
  * **問題中心のGraph RAG（GNN-KT可視化）**: 問題ルートにおけるスキルツリーの描画を抜本的に再構築。本命の問題自身を「📍現在地（星アイコン）」とし、そこから「⬛ 測られるタスク」と「🟦 必要な知識」へ放射状にエッジが伸びる、GNN-KTの推論構造をそのまま可視化したリッチなUIへ進化。
  * **問題とモデリング動画のシームレスなマージ**: 問題ルートにおいて、本命の問題文と、その問題を直接解説している先生の動画（`exercise_walkthrough`）を別々の枠に分けず、1つのコンテナ内に統合して表示するUX改善を実施。
  * **スマートラベル・クレンジング**: バックエンドデータに含まれる「確認問題」や「大問」といった文字列をフロントエンド側で自動クレンジング（除去）し、二重表示の視覚的ノイズを解消。

## [14.5.0] - 2026-08-12
### 🚀 Added (追加)
* **[Frontend] AIチューターUIのアップデート (Ver 4.5)**:
  * バックエンドのVer 14.5アップデートに伴い、「大問（Exercises）」と「確認問題（Questions）」の完全分離オントロジーをUIに直接マッピングする改修を実施。
  * **概念ルートの再定義**: 概念の検索結果画面において、「第一アクション(インプット講義) ➔ 第二アクション(大問によるモデリング) ➔ 第三アクション(確認問題によるアセスメント)」という一連の王道学習体験を提供するUIに進化。
  * **主従動画の視覚的バッジ分離**: 検索結果および関連動画リストにおいて、`concept_introduction` 等は「💡 【メイン解説】」、`prerequisite_review` は「⏪ 【前提・復習】」としてバッジを出し分ける機能を搭載。
* **[Frontend] ハイブリッド検索のインテント強制補正**:
  * クエリ内に「について」などの概念意図キーワードと、「問題」などの問題意図キーワードが混在しスコアが相殺されてしまった場合、ベクトル類似度に関わらず明示的キーワードを優先して「問題・解法ステップ優先ルート」へ強制的に分岐させるロジックを追加。
* **[Phase 1] 「大問（モデリング）」と「確認問題（アセスメント）」の完全分離抽出**:
  * `phase1_text_analysis_ontology.py` の抽出スキーマを改修し、先生によるモデリングの場である「大問（`exercises`）」と、生徒が自力で解く「確認問題（`questions`）」に厳格に分離して抽出・アライメントを行うよう機能拡張。
* **[Phase 1] 大問からの積極的なタスク・再構成抽出の解禁**:
  * 旧仕様の「大問の解説動画からは概念抽出をしない」という縛りを廃止。大問の解法を通じて知識の組み合わせ方や新しい視点が示され、新たなタスクや再構成が見られた場合はポジティブに抽出するルールを追加。
* **[Phase 1] 抽出対象テキスト（コンテキスト）の尊重ガードレール**:
  * テキストに記載された数式表現（整式など）を過去リストに引きずられずに忠実にタスク名に反映させるようプロンプトをチューニング。
* **[DB構築] 分離された問題群とリッチ・リレーション配列のDB格納**:
  * `build_vector_db.py` を改修し、分離された `exercises` と `questions` をそれぞれ独立してベクトル化し、配列データのままベクトルDBに格納（Array Ready）する仕様へアップデート。
* **[Obsidian] モデリングとアセスメントの視覚的分離 ＆ 双方向リンク対応**:
  * `export_global_obsidian_vault_mext.py` において、`node/exercise`（大問）と `node/question`（確認問題）を別々のセクションとして出力し、GNN-KTの構造を双方向リンクとしてブラウジング可能に改修。

## [14.4.0] - 2026-08-11
### 🚀 Added (追加)
* **[System] 本番環境（シラバス連携）に向けた変数名の統一 (`lecture_name`)**:
  * `bundle_name` という変数名で扱っていた講義名文字列を、本番環境のドメインモデルとのコンフリクトを回避するため、すべて `lecture_name`（講義名）という変数名およびJSONキーに統一するリファクタリングを実施。
* **[Obsidian] 講義名ハブページの自動生成とUI改善**:
  * `lecture_name`（スタディサプリの講義名）をタイトルとする独立したハブページ（タグ: `node/lecture_name`）を自動生成するロジックを追加し、ある講義に属するすべての要素を一覧表示できる強固なネットワーク構造を実現。

## [14.1.0] - 2026-08-11
### 🚀 Added (追加)
* **[DB構築] メタデータ上の知識/技能の完全分離**:
  * `build_vector_db.py` において、`foundation_knowledge` の Pillar 属性を「知識及び技能」から「知識」へと変更。動詞である「技能（タスク）」と名詞である「知識」をシステムメタデータ上でも完全に分離。

## [14.0.0] - 2026-08-11
### 🚀 Added (追加)
* **[Phase 1] オントロジーの純化 (Paradigm Shift Extraction)**:
  * `derived_knowledge`（再構成知識）の抽出基準を厳格化。「定数としての文字の扱い」のようなパラダイムシフトのみを抽出するようプロンプトを改修。
* **[Phase 3] 動画アライメントの主従分離**:
  * `alignment_type` を細分化し、概念自体の説明を `concept_introduction`（主）、別のタスクを行っている過程での言及を `prerequisite_review`（従）として厳密に分類させるルールを追加。

---

*(以下、Ver 13.x 以前の履歴は省略)*