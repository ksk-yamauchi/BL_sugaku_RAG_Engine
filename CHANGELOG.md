## 履歴とバージョン管理 (Changelog)

本ドキュメントは、スタサプRAGエンジンおよび次世代AIチューターシステムの機能追加、変更、修正の履歴を管理します。
バージョニングは「Keep a Changelog」の思想に基づき、各フェーズのスクリプト群および出力データの仕様変更を記録します。

---

## [12.2.0] - 2026-08-04
### 🚀 Added (追加)
* **[System] 複数APIキーの自動ローテーション機構**:
  * `.env` ファイルにて `GEMINI_API_KEYS`（カンマ区切り）で複数キーを登録可能に拡張。
  * 各Phaseスクリプト（0〜3）にて、APIの利用制限（HTTP 429）や無効なキー（HTTP 400）を検知した場合、待機せずに自動で次のAPIキーへ切り替えて処理を続行する堅牢なフェイルオーバーを実装。
* **[System] 動的リトライ回数設定**:
  * 固定の最大リトライ回数（5回）を廃止し、登録されたAPIキーの数に応じて動的に最大リトライ回数を設定（例: `max(5, キー数 × 2)`）する仕様に変更。
* **[Phase 1, 2, 3] JSON自動修復・パース機能**:
  * LLM（Gemini）がJSON出力時にLaTeXのバックスラッシュ（`\`）のエスケープを忘れたことに起因する `JSONDecodeError` のクラッシュを防ぐため、システム側でテキストを自動検知・修復（`\` ➔ `\\`）してからパースを行う安全装置（`generate_content_and_parse_json`）を全解析フェーズに搭載。
* **[Phase 0, 2] ファイル再アップロード（リカバリー）機構**:
  * 動画（Phase 2）およびPDF（Phase 0）の解析中、APIキーが切り替わったことによって発生するクラウド上の一時ファイルへのアクセス権限エラー（HTTP 403 PERMISSION_DENIED）を自動検知。
  * 403エラー発生時、無限ループに陥るのを防ぎ、自動で古いファイルを破棄して新しいキーで再アップロードからやり直す自己修復ループを実装。
* **[Document] GNN-KTシミュレーション仕様書の追加**:
  * ペルソナ定義および、必須/補足の重み付き前提知識を用いた連鎖ペナルティのアルゴリズムをまとめた専用仕様書（`docs/08_mock_log_simulation_spec.md`）を新規作成。

### 🔄 Changed (変更)
* **[System] 環境変数の強制上書き仕様**:
  * Pythonの `load_dotenv` において、ターミナルセッション等に残存する古い環境変数を確実に上書きするため `override=True` 設定を追加。さらに、BOM（`\ufeff`）やクォーテーションを自動除去するクレンジング関数を初期化処理に導入。
* **[Phase 0, 2] クラウドキャッシュ削除の安定化**:
  * 処理完了時のファイル削除（`files.delete`）処理において、アップロード時と同一のクライアントインスタンスを保持して使用するよう変更し、キー切り替え時の不要な削除エラーを抑止。
* **[Backend] GNN-KT検証用ダミーログ生成ロジックの高度化**:
  * `generate_mock_logs.py` において、最新のオントロジー構造に対応。必須前提（`mandatory`）未理解時は正答率×0.1、補足前提（`supplementary`）未理解時は×0.7とする連鎖ペナルティ計算を実装し、よりリアルなクラスの躓きデータを生成するよう改修。

---

## [12.1.0] - 2026-08-04
### 🚀 Added (追加)
* **[Phase 1] 枝番マスターメタデータの拡張・自動蓄積**:
  * `concept_branch_master.json` の各概念ノードに、要約の冒頭（`summary_snippet`）および初出単元名（`first_appeared_in`）を追加保存する仕様へ変更。
  * 後続の講（PART）の解析において過去の定義コンテキストを保持可能にし、全講を通したオントロジーの一貫性を向上。
* **[Backend] シラバス可視化テストツールの対話型CLI化**:
  * ターミナル上でユーザーが直接シラバステキストを手入力し、紐づく「Level 3親概念 ➔ Level 4アクション ➔ 動画ファイルおよび問題数」をリアルタイムで繰り返し確認できる対話型検証スクリプト（`visualize_syllabus_content.py`）を追加。
* **[Backend] GNN-KT検証用ダミーログ生成ツールの追加**: 
  * 統合データベース（`global_vector_db_cache.json`）のオントロジー構造を直接読み込み、架空の生徒ごとの正誤ログデータ（`mock_student_logs.json`）を自動生成する `generate_mock_logs.py` を新規作成。
  * 「知識・技能偏重型」などのペルソナ設定や、必須前提知識の不理解による「連鎖エラー」の確率変動ロジックを搭載し、GNN-KTのネットワーク推論や適応型学習ナビの検証に耐えうるリアリティのあるデータをシミュレーション可能にした。
* **[Document] 画像RAG専用プロンプト仕様書の追加**: 
  * マルチモーダル画像RAGにおいて、画像をスタサプのオントロジー（Level 3推論、前提知識、エラー分析）にマッピングするためのJSONスキーマとシステムプロンプトの設計書（`docs/06_image_rag_prompt_design.md`）を新規作成。
* **[Document] シラバスマッチング専用プロンプト仕様書の追加**:
  * 学校独自のシラバス表記をスタサプオントロジー（Level 3）にマッピングするためのシステムプロンプト設計書（`docs/07_syllabus_mapping_prompt.md`）を新規作成。

### 🔄 Changed (変更)
* **[Phase 1] In-Context Learning（Few-Shot）によるプロンプト構造改革**:
  * 枝番マスターに蓄積された過去の概念名と要約スニペットを、Phase 1（Step 1）実行時に「お手本データ」としてプロンプトへ動的注入するロジック（`load_branch_master_examples`）を搭載。
  * AIの萎縮や解釈のブレを引き起こしていた「NGルール（〜するな）」や曖昧な定性表現（「日常的な授業で使われる」等）を完全撤廃し、ポジティブな形式ルール（`[対象]＋[具体的処理]`）と実例模倣のみで教科書レベルの概念名を抽出する仕様へ改修。

---

## [12.0.0] - 2026-08-03
### 🚀 Added (追加)
* **[Phase 1] GNN-KTおよびSemantic Zoom用データ構造の追加**: 
  * 抽出概念（Level 4）に対して、学習観点タグ（`competency`: 知識・技能 / 思考力・判断力等）の自動分類を追加。
  * 前提知識（`prerequisite_concepts`）を単純な文字列から、`dependency_type`（mandatory[必須] / supplementary[補足]）と `reasoning`（理由）を持つオブジェクト構造へ拡張。
* **[Frontend] Graph RAG UIの拡張**:
  * `app.py` のスキルツリー（`agraph`）にて、必須前提（実線／濃い青）と補足前提（点線／水色）を視覚的に分離。
  * `app.py` のサイドバーに、現在読み込んでいるDBのバージョン（`engine_version`）を表示するUIを追加し、データ仕様の視認性を向上。
* **[Frontend] FT画面モックアップの追加とUI高度化**:
  * 先生向けダッシュボード `app_ft.py` を新規作成。階層型ヒートマップ（Treemap）を用いたSemantic Zoomのプロトタイプを実装。
  * 評価指標を教育現場に即した「習熟度」に統一し、対象スコープ（学年・クラス等）に連動するAI根本原因アラートを実装。
  * ヒートマップのホバー時に少し遅れて情報を表示するディレイ機能の仕様を追加し、Level 4のブロックをクリックした際に直下の「学習アクション・エクスプローラー（展開パネル）」が自動で開く（または内容が更新される）連動UIを設計。
  * 「数学担当による全体分析（メインダッシュボード）」と「担任による面談時の個別配信（面談ダッシュボード）」の2つのユースケースを疑似体験できるタブ切り替え機能（状態保持つき）を実装。
  * 既存システムの「STEP2: 配信設定」画面へのシームレスな遷移モックを追加。習熟度に基づく対象生徒の自動絞り込み、宿題タイプの自動選択、および遡り学習パック内容の可視化・編集（削除）機能を実装。
* **[System] 共通環境構築ファイルの追加**:
  * プロジェクトルート（`BL_sugaku/`）に共通の `requirements.txt` を配置し、フロント・バックエンド両方の環境構築手順を統合・標準化。
* **[Document] フロントエンド仕様書の統合・新規作成**:
  * 独立して存在していた `app.py` の仕様書と新規作成した `app_ft.py` の仕様を統合し、新たに `docs/05_frontend_applications.md` としてドキュメントを追加。

### 🔄 Changed (変更)
* **[Phase 1] 前提知識の閉世界化（厳密化）**: プロンプトを改修し、前提知識として指定できる用語をLevel 3（親概念ハブ）レベルの一般数学用語に限定。表記揺れとグラフの分断を防止。
* **[DB構築]**: `build_vector_db.py` を改修し、メタデータに観点タグと重み付き前提知識を包含してベクトル空間へ埋め込む仕様に変更。また、出力JSONのメタデータに `engine_version` を刻む仕様を追加。
* **[Obsidian]**: `export_global_obsidian_vault_mext.py` を改修し、Level 3の親概念ハブノードを自動集計・出力する仕様に変更。ターミナル出力のバージョン表記も「Ver 12.0」へ更新。
* **[Phase 0]**: `phase0_pdf_to_md.py` の開始ターミナルログに `[Phase 0 Ver 1.0]` と明記するよう変更。

### 🛠️ Fixed (修正)
* **[Phase 3] OCRデータの引き継ぎ保証 (ファジーマッチ結合)**: Phase 2（Ver 2.5）で抽出された `blackboard_ocr` などのデータを、新しいPhase 1データと結合する際に欠落せず安全に引き継ぐ処理を実装。

---

## [10.4.0] - 以前の主要アップデート
### 🚀 Added (追加)
* **[Phase 3] ファジーマッチ（最寄時間検索）安全装置**: タイムスタンプのミリ秒単位のズレや、欠落が発生した場合でも、一番近い時間のセグメントを自動で探して動画と概念を結合するフォールバックロジックを搭載。

---

## [2.5.1] - 2026-08-03
### 🛠️ Fixed (修正)
* **[Phase 2] トピック名の正規化ロジック修正**: `concept_application`（応用・利用）の動画セグメントまで「[例題]」フォーマットに強制上書きされてしまう要件定義の誤りを修正。トピック名の正規化処理を `exercise_walkthrough`（例題演習）のみに限定し、正しい判定ログ（「例題と判定されたため...」）が出力されるよう改修。

---

## [2.5.0] - 以前の主要アップデート
### 🚀 Added (追加)
* **[Phase 2] マルチモーダル超極細解析＆板書OCR**: Gemini 1.5 Pro (当時) / 3.6 Flash のマルチモーダル機能を活かし、動画内の黒板・スライドの数式をLaTeXとして書き起こす `blackboard_ocr` 機能を実装。
* **[Phase 2] 動的役割推論（ロール判定）**: 教材テキストと連動し、講義動画の役割を `concept_lecture`（概念講義）、`exercise_walkthrough`（例題演習）、`concept_application`（応用・利用）の3つに自動分類するロジックを搭載。