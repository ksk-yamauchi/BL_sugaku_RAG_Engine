## 履歴とバージョン管理 (Changelog)

本ドキュメントは、スタサプRAGエンジンおよび次世代AIチューターシステムの機能追加、変更、修正の履歴を管理します。
バージョニングは「Keep a Changelog」の思想に基づき、各フェーズのスクリプト群および出力データの仕様変更を記録します。

---
## [14.6.0] - 2026-08-12
### 🚀 Added (追加)
* **[Frontend] AIチューターUIのメジャーアップデート (Ver 4.6 Array Ready)**:
  * **多段・配列引き当てロジックの導入**: 単一の代表概念（`matched_concept`）への依存を脱却し、問題データが保持するタスク配列（`linked_task_names`）と知識配列（`linked_knowledge_names`）の包含関係を活用した確実な引き当て機構を実装。これにより、検索結果がタスクノードだった場合の「関連問題の空振り」を完全に解消。
  * **タスク駆動型の類題サジェスト（横展開）**: 問題ルートの「第二アクション（同概念の横展開演習）」において、単に知識が同じ問題ではなく、「同じタスク（技能）を測る問題」を優先して類題として結びつける教育的アルゴリズムを採用。
  * **問題中心のGraph RAG（GNN-KT可視化）**: 問題ルートにおけるスキルツリーの描画を抜本的に再構築。本命の問題自身を「📍現在地（星アイコン）」とし、そこから「⬛ 測られるタスク」と「🟦 必要な知識」へ放射状にエッジが伸びる、GNN-KTの推論構造をそのまま可視化したリッチなUIへ進化。
  * **問題とモデリング動画のシームレスなマージ**: 問題ルートにおいて、本命の問題文と、その問題を直接解説している先生の動画（`exercise_walkthrough`）を別々の枠に分けず、1つのコンテナ内に統合して表示するUX改善を実施。
  * **スマートラベル・クレンジング**: バックエンドデータに含まれる「確認問題」や「大問」といった文字列をフロントエンド側で自動クレンジング（除去）し、UI上のプレフィックスと二重表示（例：「📗 確認問題 確認問題 3」）になってしまう視覚的ノイズを解消。
  
## [14.6.0] - 2026-08-12
### 🚀 Added (追加)
* **[Frontend] AIチューターUIのメジャーアップデート (Ver 4.6 Array Ready)**:
  * **多段・配列引き当てロジックの導入**: 単一の代表概念（`matched_concept`）への依存を脱却し、問題データが保持するタスク配列（`linked_task_names`）と知識配列（`linked_knowledge_names`）の包含関係を活用した確実な引き当て機構を実装。これにより、検索結果がタスクノードだった場合の「関連問題の空振り」を完全に解消。
  * **タスク駆動型の類題サジェスト（横展開）**: 問題ルートの「第二アクション（同概念の横展開演習）」において、単に知識が同じ問題ではなく、「同じタスク（技能）を測る問題」を優先して類題として結びつける教育的アルゴリズムを採用。
  * **問題中心のGraph RAG（GNN-KT可視化）**: 問題ルートにおけるスキルツリーの描画を抜本的に再構築。本命の問題自身を「📍現在地（星アイコン）」とし、そこから「⬛ 測られるタスク」と「🟦 必要な知識」へ放射状にエッジが伸びる、GNN-KTの推論構造をそのまま可視化したリッチなUIへ進化。
  * **問題とモデリング動画のシームレスなマージ**: 問題ルートにおいて、本命の問題文と、その問題を直接解説している先生の動画（`exercise_walkthrough`）を別々の枠に分けず、1つのコンテナ内に統合して表示するUX改善を実施。
  * **スマートラベル・クレンジング**: バックエンドデータに含まれる「確認問題」や「大問」といった文字列をフロントエンド側で自動クレンジング（除去）し、UI上のプレフィックスと二重表示（例：「📗 確認問題 確認問題 3」）になってしまう視覚的ノイズを解消。

## [14.6.0] - 2026-08-12
### 🚀 Added (追加)
* **[Frontend] AIチューターUIのメジャーアップデート (Ver 4.6 Array Ready)**:
  * **多段・配列引き当てロジックの導入**: 単一の代表概念（`matched_concept`）への依存を脱却し、問題データが保持するタスク配列（`linked_task_names`）と知識配列（`linked_knowledge_names`）の包含関係を活用した確実な引き当て機構を実装。これにより、検索結果がタスクノードだった場合の「関連問題の空振り」を完全に解消。
  * **タスク駆動型の類題サジェスト（横展開）**: 問題ルートの「第二アクション（同概念の横展開演習）」において、単に知識が同じ問題ではなく、「同じタスク（技能）を測る問題」を優先して類題として結びつける教育的アルゴリズムを採用。
  * **問題中心のGraph RAG（GNN-KT可視化）**: 問題ルートにおけるスキルツリーの描画を抜本的に再構築。本命の問題自身を「📍現在地（星アイコン）」とし、そこから「⬛ 測られるタスク」と「🟦 必要な知識」へ放射状にエッジが伸びる、GNN-KTの推論構造をそのまま可視化したリッチなUIへ進化。
  * **問題とモデリング動画のシームレスなマージ**: 問題ルートにおいて、本命の問題文と、その問題を直接解説している先生の動画（`exercise_walkthrough`）を別々の枠に分けず、1つのコンテナ内に統合して表示するUX改善を実施。
  * **スマートラベル・クレンジング**: バックエンドデータに含まれる「確認問題」や「大問」といった文字列をフロントエンド側で自動クレンジング（除去）し、UI上のプレフィックスと二重表示（例：「📗 確認問題 確認問題 3」）になってしまう視覚的ノイズを解消。

## [14.5.0] - 2026-08-12
### 🚀 Added (追加)
* **[Frontend] AIチューターUIのアップデート (Ver 4.5)**:
  * バックエンドのVer 14.5アップデートに伴い、「大問（Exercises）」と「確認問題（Questions）」の完全分離オントロジーをUIに直接マッピングする改修を実施。
  * **概念ルートの再定義**: 概念の検索結果画面において、「第一アクション(インプット講義) ➔ 第二アクション(大問によるモデリング) ➔ 第三アクション(確認問題によるアセスメント)」という一連の王道学習体験を提供するUIに進化。知識ノードであっても、リンク遷移なしで紐づく問題を直接展開可能に。
  * **主従動画の視覚的バッジ分離**: 検索結果および関連動画リストにおいて、`concept_introduction` 等は「💡 【メイン解説】」、`prerequisite_review` は「⏪ 【前提・復習】」としてバッジを出し分ける機能を搭載。
* **[Frontend] ハイブリッド検索のインテント強制補正**:
  * クエリ内に「について」「教えて」などの概念意図キーワードと、「問題」「演習」などの問題意図キーワードが混在しスコアが相殺されてしまった場合、ベクトル類似度に関わらず「問題」等の明示的キーワードを優先して「問題・解法ステップ優先ルート」へ強制的に分岐させるロジックを追加し、UXの違和感を解消。
* **[Phase 1] 「大問（モデリング）」と「確認問題（アセスメント）」の完全分離抽出**:
  * `phase1_text_analysis_ontology.py` の抽出スキーマを改修し、従来は `questions` という単一の配列に混在していた問題を、先生によるモデリングの場である「大問（`exercises`）」と、生徒が自力で解く「確認問題（`questions`）」に厳格に分離して抽出・アライメントを行うよう機能拡張。
  * これにより、大問の解法を通じた「新しいタスクや再構成の誕生」と、確認問題を通じた「知識の定着度アセスメント」という、教育的な役割の違いをシステム上で表現・区別することが可能となった。
* **[Phase 1] 大問からの積極的なタスク・再構成抽出の解禁**:
  * 旧仕様の「大問の解説動画からは概念抽出をしない」というレガシーな縛りを廃止。大問の解法を通じて知識の組み合わせ方や新しい視点が示され、新たなタスクや再構成が見られた場合はポジティブに抽出するルールを追加。
* **[Phase 1] 抽出対象テキスト（コンテキスト）の尊重ガードレール**:
  * AIの「過去の記憶（オントロジー・メモリ）」への適合が強すぎるあまり、「整式」が「多項式」へ強引に統合されてしまう問題を解決するため、第3のルール「タスク命名における対象テキストの尊重」を追加。テキストに記載された数式表現（整式など）を過去リストに引きずられずに忠実にタスク名に反映させるようプロンプトをチューニング。
* **[DB構築] 分離された問題群とリッチ・リレーション配列のDB格納**:
  * `build_vector_db.py` を改修し、分離された `exercises` と `questions` をそれぞれ独立してベクトル化。さらに、問題がどのタスク・知識に紐づいているかを不可逆圧縮せずに `linked_task_names` および `linked_knowledge_names` という配列データのままベクトルDBに格納（Array Ready）する仕様へアップデート。
* **[Obsidian] モデリングとアセスメントの視覚的分離 ＆ 双方向リンク対応**:
  * `export_global_obsidian_vault_mext.py` において、`node/exercise`（大問）と `node/question`（確認問題）を別々のセクションとして出力。さらに、問題が持つタスク・知識の配列を展開し、Obsidian上でGNN-KTの構造を双方向リンク（`[[ ]]`）としてブラウジング可能に改修。

## [14.4.0] - 2026-08-11
### 🚀 Added (追加)
* **[System] 本番環境（シラバス連携）に向けた変数名の統一 (`lecture_name`)**:
  * これまで `bundle_name` という変数名で扱っていた講義名文字列を、本番環境のドメインモデルとのコンフリクトを回避するため、すべて `lecture_name`（講義名）という変数名およびJSONキーに統一するリファクタリングを実施。
  * `build_index_master.py` を改修し、出力される `lecture_index_master.json` のキーを `bundle_name` から `lecture_name` に変更。
  * `phase1_text_analysis_ontology.py`, `phase3_alignment_graph.py`, `build_vector_db.py` 内の変数名およびメタデータキーをすべて `lecture_name` に置換。ベクトル化の際の合成テキストにも「【講義名】」として埋め込むよう仕様変更。
* **[Obsidian] 講義名ハブページの自動生成とUI改善**:
  * `lecture_name`（スタディサプリの講義名）をタイトルとする独立したハブページ（タグ: `node/lecture_name`）を自動生成するロジックを追加し、ある講義に属するすべての要素を一覧表示できる強固なネットワーク構造を実現。
  * 各ノード（知識、タスク、問題、動画）のMarkdownに、このハブページへのリンク（`🎓 スタディサプリの講義名: [[...]]`）を直接出力するよう改修し、問題からの逆引きという非効率なロジックを完全破棄。これにより、Obsidian上で「ある講義に属するすべての要素」を一覧表示できる強固なネットワーク構造を実現。

## [14.1.0] - 2026-08-11
### 🚀 Added (追加)
* **[DB構築] メタデータ上の知識/技能の完全分離**:
  * `build_vector_db.py` において、`foundation_knowledge` の Pillar 属性を「知識及び技能」から「知識」へと変更。動詞である「技能（タスク）」と名詞である「知識」をシステムメタデータ上でも完全に分離。

## [14.0.0] - 2026-08-11
### 🚀 Added (追加)
* **[Phase 1] オントロジーの純化 (Paradigm Shift Extraction)**:
  * `derived_knowledge`（再構成知識）の抽出基準を厳格化。「タスクを単に名詞化しただけの結果論」の抽出を禁止し、「定数としての文字の扱い」のようなパラダイムシフトのみを抽出するようプロンプトを改修。
* **[Phase 3] 動画アライメントの主従分離**:
  * `alignment_type` を細分化し、概念自体の説明を `concept_introduction`（主）、別のタスクを行っている過程での言及を `prerequisite_review`（従）として厳密に分類させるルールを追加。

---

*(以下、Ver 13.x 以前の履歴は省略)*